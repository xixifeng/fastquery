查询返回 List<实体> 待续...

校验: Page<T> T 要么是List<Map> 要么是一个实体

分页SQL中禁止出现limit @FirstResult @MaxResults 禁止重复出现
分页方法必须传递Pageable参数
查操作@Query不能重复
PageImple的实现需要去掉 用asm来动态实现.

分页@Query 中的  countQuery 求和语句...
参考, 还需要参考jfianl
https://github.com/spring-projects/spring-data-commons/blob/master/src/main/java/org/springframework/data/domain/Slice.java
https://github.com/spring-projects/spring-data-commons/blob/master/src/main/java/org/springframework/data/domain/Page.java
https://github.com/spring-projects/spring-data-commons/blob/master/src/main/java/org/springframework/data/domain/Pageable.java

1.集成: querydsl http://www.querydsl.com/
 
有待酌情优化的
http://www.ibm.com/developerworks/cn/webservices/1109_yangw_asm/ asm遍历类...
改操作的返回值可能是List<int> List<Map> List<JSONObject> List<Primarykey>,List<boolean>,List<Bean> 还有可能时数组类型 暂不考虑.如果全部考虑有点复杂


			// ---- 把字节存储到硬盘 ----------
			try {
				FileOutputStream fileOutputStream = new FileOutputStream("/tmp/tmp-root/" + repositoryClazz.getSimpleName()+suffix +".class");
				fileOutputStream.write(bytes);
				fileOutputStream.flush();
				fileOutputStream.close();
			} catch (IOException e) {
				e.printStackTrace();
			}
			// ---- 把字节存储到硬盘 ---------- End
			
			
5. fastquery.json 数据源的作用范围定位到包地址,如果没有指定数据源的范围应该提示. 精准到类名也支持.
 
@Transactional支持 
有些表如果不支持事务,那么返回确实已经修改的行数(有待解决)

 重要: 仅仅只是在 Repository 生成类的时候依赖的方法,都需要进行保护起来. (待续..................)
 
 3.sql与代码分离 参考@QueryByName 或者 @NamedQuery。